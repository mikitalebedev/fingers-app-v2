<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--<uses-permission android:name="android.permission.INTERNET" />-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera.front" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme"
        tools:targetApi="m">
        <activity
            android:name=".SplashActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LearningLettersActivity" />
        <activity android:name=".LearningActivity" />
        <activity android:name=".GamePlayActivity" />
        <activity android:name=".GameResultActivity" />
        <activity android:name=".ResultChooseSign" />
        <activity android:name=".SpeechToTextActivity" />
        <activity android:name=".TrainingActivity"
            android:theme="@style/AppTheme.Recognition" /> <!-- Применил новую тему здесь -->
        <activity android:name=".RecognitionActivity"
        android:theme="@style/AppTheme.Recognition"/> <!-- Применил новую тему здесь -->
        <activity android:name=".GameChooseSign" />
        <activity android:name=".SignLanguageActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".GameChooseLetter"/>
        <activity android:name=".TheoryActivity"/>
        <activity android:name=".TeoryWords"/>
        <activity android:name=".EndActivity"/>
        <activity android:name=".RegistrationActivity"/>



        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
